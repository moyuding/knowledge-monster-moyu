# Nixpacks 配置文件 - 修复版
# 使用小写命令，明确路径

[phases.setup]
cmds = [
    "cd knowledge-monster/backend && npm install"
]

[phases.build]
cmds = [
    "echo 'Build completed'"
]

[start]
cmd = "cd knowledge-monster/backend && npm start"